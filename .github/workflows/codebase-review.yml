name: AI Code Review (Template)

# This is a TEMPLATE workflow for AI code review integration
# Uncomment and configure when ready to enable AI review

# See docs/guides/ai-code-review.md for setup instructions

on:
  pull_request:
    types: [opened, synchronize, reopened]

# Uncomment when ready to enable
# jobs:
#   ai-review:
#     name: AI Code Review
#     runs-on: ubuntu-latest
#     if: github.event.pull_request.draft == false
#     timeout-minutes: 10
#
#     permissions:
#       contents: read
#       pull-requests: write
#
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#
#       # Option 1: GitHub Copilot Code Review
#       # - name: Copilot Code Review
#       #   uses: github/copilot-code-review@v1
#       #   with:
#       #     github-token: ${{ secrets.GITHUB_TOKEN }}
#
#       # Option 2: Claude AI Code Review
#       # - name: Claude AI Review
#       #   uses: anthropics/claude-code-review@v1
#       #   with:
#       #     anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
#       #     model: claude-sonnet-4.5
#       #     focus-areas: |
#       #       - Effect-TS best practices
#       #       - Type safety
#       #       - Error handling patterns
#       #       - Performance issues
#
#       # Option 3: Custom AI Review Script
#       # - name: Setup Bun
#       #   uses: oven-sh/setup-bun@v2
#       #
#       # - name: Install dependencies
#       #   run: bun install --frozen-lockfile
#       #
#       # - name: Run custom AI review
#       #   env:
#       #     ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
#       #   run: bun run scripts/ai-review.ts
#
#       # Option 4: CodeRabbit AI
#       # - name: CodeRabbit Review
#       #   uses: coderabbitai/coderabbit-action@v1
#       #   with:
#       #     github-token: ${{ secrets.GITHUB_TOKEN }}
#       #     openai-api-key: ${{ secrets.OPENAI_API_KEY }}
